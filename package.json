{
  "name": "mop-calendar",
  "version": "1.3.0",
  "description": "MOP Google Calendar 자동 등록 프로그램",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build-win": "electron-packager . \"MOP Calendar\" --platform=win32 --arch=x64 --out=dist --overwrite --electron-version=28.3.3"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-packager": "^17.1.2"
  }
}
